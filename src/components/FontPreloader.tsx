import React from 'react';

/**
 * FontPreloader component - minimal version
 * Most font optimizations now happen at the HTML transform level
 */
const FontPreloader: React.FC = () => {
  return null;
};

export default FontPreloader; 